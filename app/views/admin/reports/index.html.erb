<h1><%= t(:listing_reports).capitalize %></h1>

<table>
  <tr>
    <th><%= t(:description).capitalize %></th>
    <th><%= t(:fields).capitalize %></th>
    <th><%= t(:user).capitalize %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% admin_reports.each do |admin_report| %>
  <tr>
    <td><%= admin_report.description %></td>
    <td><%= admin_report.fields %></td>
    <td><%= admin_report.user %></td>
    <td><%= link_to t(:show), admin_report %></td>
    <td><%= link_to t(:delete), admin_report, confirm: t(:are_you_sure), method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

